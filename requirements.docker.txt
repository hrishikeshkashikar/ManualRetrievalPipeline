# API Framework
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.9

# PDF Processing
PyMuPDF>=1.24.0
Pillow>=10.0.0

# Embeddings & Reranking
# Pin torch to avoid surprise mega-downloads; CPU wheels are enough for embed/rerank.
torch==2.2.2
sentence-transformers>=3.0.0,<6.0.0

# Vector Store
chromadb>=0.5.0,<1.1.0

# Ollama Client
ollama>=0.4.0

# Configuration
pydantic-settings>=2.0.0

# Utilities
python-dotenv>=1.0.0
